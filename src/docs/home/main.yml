Basic Intoductory route

---

paths:
  /home:
    get:
      summary: Returns a String.
      description: Intro 
      responses:
        200:
          description: A JSON array of strings
          content:
            application/json:
              schema: 
                type: array
                items: 
                  type: string